# core.autocrlf=true on the Windows boxes would otherwise check shell scripts out
# with CRLF, which breaks the shebang (`#!/usr/bin/env bash` -> bad interpreter).
*.sh text eol=lf
*.ps1 text eol=crlf
