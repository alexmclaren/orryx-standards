---
name: fleet-restore-verification
description: One-time verification that the rebuilt scheduler executes routines end-to-end
---

This is a one-time verification run for the scheduler registry rebuild. Read C:\Users\alexa\.claude\scheduled-tasks\fleet-health-routine\SKILL.md and execute it exactly as written (it is the canonical fleet-health routine — read-only over logs, emits a dated fleet-health report and an exit-log entry). Note in the report header that this was a manual verification run following the registry rebuild.